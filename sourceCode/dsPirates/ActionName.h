#pragma once

#include "stdafx.h"

/**This type is needed for the input system and represents the name of an action that the
player can execute by pressing keys.*/

typedef std::string ActionName;

/**This type represent the configuration file type for keyboard names.
It is more readable to use string that the actual OIS enum values.*/

typedef std::string KeyName;